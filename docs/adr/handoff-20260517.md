# Handoff — 实现 portfolio-tracking 复用机制（用 /tdd）

## 目标

把已锁定的设计实现进 `commands/portfolio-tracking.md`：让 `/analyst-deal:portfolio-tracking` 先复用分析师此前用 standalone `/financial-analyzer` 与 `/competitor-enricher` 留下的产物，而不是每季无条件 re-dispatch / re-jina。

**这是 prompt-engineering 改动（改的是命令 .md 指令文件），不是常规代码。** `/tdd` 的适配方式见下方"测试策略"。

## 设计已锁定 — 不要重新设计

完整设计与理由已固化，**先读这两份，不要再问设计问题**：

- `/Users/jackcheng/.claude/plugins/marketplaces/analyst-pro-marketplace/docs/adr/0002-cross-command-reuse-contracts.md` — 决策、被否方案、Consequences、改动范围
- `/Users/jackcheng/.claude/plugins/marketplaces/analyst-pro-marketplace/CONTEXT.md` — 术语表（务必区分：合并报表=原始输入 / 历年财务报表 xlsx=并表目标 / fin-cache 留底=可复用 prose+JSON / 报告期日期 vs 文件名 token）

设计经过一轮 `/grill-with-docs` 逐问确认，9 个问题用户全选推荐项（A）。锁定结论摘要见上一轮助手消息的"locked design"表，与 ADR 0002 一致。

## 待改文件

仅一个 runtime 文件，**双端同步**（上一个 commit 的教训：runtime 才是实际加载位）：

- canonical source：`/Users/jackcheng/.claude/plugins/marketplaces/analyst-pro-marketplace/analyst-deal/commands/portfolio-tracking.md`
- runtime（实际加载，必须同步）：`/Users/jackcheng/.claude/plugins/cache/analyst-pro-marketplace/analyst-deal/0.1.1/commands/portfolio-tracking.md`

`financial-analyzer.md` / `competitor-enricher.md` / agents / `docs/designs/fin-sidecar-contract.md`(FROZEN) **不动**。ADR/CONTEXT 仅 source 端，不入 cache。

## 改动点（章节锚定到现有 portfolio-tracking.md 步骤）

财务（Step 5 / 5.5）：

- Step 5 前加：AskUserQuestion 确认 fin-cache 扫描文件夹（默认 CWD 根、备选 `./portfolio/{slug}/`），重算 `sha8 = printf '%s' "$folder" | shasum -a 256 | cut -c1-8`，定位 `<folder>/.fin-cache/<SHA8>/`
- 命中判定：严格按 `{季度}`→期末日 token（Step 5.1 现有派生表：Q1→YYYY-03-31→`20260331`）精确匹配 `<YYYYMMDD>_section.md` **且** 其 mtime 晚于原始合并报表 PDF（鲜度）
- 命中且鲜度 OK → 一个 AskUserQuestion（默认 A=复用 section.md 填 FINANCIAL_PLACEHOLDER + 不跑子 agent / B=丢弃重抽走现有 Step 5.2 dispatch）；miss 或陈旧 → **静默**走现有 Step 5.2，不新增问题
- Step 5.5：复用时把 fin-cache `<YYYYMMDD>.json` 拷贝/指为 `./portfolio/{slug}/current_quarter_financials.json`，照常并表；缺 JSON → 跳 5.5 并 Output 标注「未同步历年表」

竞对（Step 2 / 6 / 8 / 9）：

- Step 2 competitors.yml schema：顶层加全局有序 `档案搜索路径:`，默认 `[./, ./portfolio/{slug}/competitors/]`
- Step 6：对每家**有序回退链**——路径1未命中→路径2→都没→jina；匹配=文件名含 name-slug（大小写不敏感、允许 `NN_` 前缀）优先，0/歧义则 grep `#### …<公司名>` 表头兜底
- Step 6 前：先跑一遍匹配，**一个批级** AskUserQuestion 含每路径命中数 + 将走 jina 的逸名预览（A=按预览走 / B=改搜索路径重扫 / C=全 jina）
- Step 8/Output：逐家标注来源路径 + card 内查询日期 + 是否含 `jina 不可用 / 数据缺口 / 数据完整性声明 / 未核验` 关键词（原样复用，不阻断）
- Step 9：把确认后的 `档案搜索路径` 列表回写 competitors.yml

硬约束（已在该命令文件内、勿违反）：所有新增 AskUserQuestion 必须符合文件内"## AskUserQuestion 调用契约（硬约束）"——非空 `questions`、每项 ≥2 `options`、不只回显 ASCII、不开计划外确认。复用 miss 一律静默回退，**不**新增计划外问题。

## 测试策略（/tdd 适配 prompt 改动）

无单测 runner。把"红"定义为可复现的命令行为缺陷，"绿"为修订指令后该场景走对。建议反馈环（参考 /diagnose Phase 1 思路）：

1. 用一个 fixture 目录（仿当前真实场景）：`<dir>/.fin-cache/<sha8(dir)>/20260331_section.md` + `20260331.json`，外加一个 `*合并报表*.pdf`（mtime 早于留底）、6 个 `0N_<name>.md` 竞对 card（含 05/06 带 ⚠️ banner 的）、一个 competitors.yml。
2. 每个验收场景写成一句可判定断言，逐条对修订后的指令做 dry-run 推演（人审 + 必要时实跑 `/analyst-deal:portfolio-tracking`）：
   - 命中+鲜度OK → 弹复用闸门、默认 A、选 A 后 section.md 进报告且不 dispatch agent
   - 文件名 token≠报告期（如缓存名 `20260429`）→ 严格匹配 miss → 静默 dispatch（**不**报 InputValidationError，不弹计划外问题）
   - 留底比 PDF 旧 → 判陈旧 → 静默 dispatch
   - 缺 JSON 只有 section.md → 复用正文但跳 5.5 + Output 标注
   - 竞对：4 家命中 CWD 根、2 家仅在 portfolio/competitors/、本批演示 05/06 不在任何路径 → 预览正确、逐家回退、仅缺失家走 jina
   - 05/06 复用 → Output 显式列出 ⚠️ 关键词与来源路径
3. 每改一处指令，重跑全部场景断言（回归）。

验收门槛：上述场景全绿 + 现有未触及步骤行为不回归 + AskUserQuestion 调用契约不被新问题违反。

## 关键背景 / 陷阱

- jina 配额：本会话 batch 2（龙芯/景嘉微）撞 `API quota exhausted`，故 `05_龙芯中科.md`/`06_景嘉微.md` 是训练知识回退、带 ⚠️ 数据完整性声明。复用治理决策（原样复用+Output 标注）正是为此类 card。
- "文件名日期 ≠ 报告期"是本工作起源 bug（standalone financial-analyzer 文件名 `20260429` vs 报告期 `20260331` 触发 InputValidationError）；上一个 commit `69266f2` 已在 financial-analyzer.md/portfolio-tracking.md 加 AskUserQuestion 契约 + 文件名日期交叉校验。本次复用的"严格期末日匹配"是同一防线的延伸——勿放宽成模糊匹配。
- 当前会话产物 6 个竞对 card 在 `/Users/jackcheng/Documents/00_已投项目/2021年 格兰菲项目/7、投后管理/3、项目投后管理执行材料/20260513-2026年第一季度跟进/`（CWD 根，即 `01_英伟达.md`…`06_景嘉微.md`），可直接当竞对复用 fixture。
- HITL：改完不要自动 commit/push，除非用户明确要求；提示用户审阅后再定。
- sha8 公式必须与 standalone financial-analyzer 完全一致：`printf '%s' "$abs_folder" | shasum -a 256 | cut -c1-8`（见原始 trace 与 fin-sidecar-contract.md 的 `.fin-cache/<sha8(abs_folder)>/`）。

## 下一步建议技能

1. `/tdd` — 主驱动（按上方"测试策略"适配 prompt 改动）
2. 实现后可选 `/analyst-deal:portfolio-tracking 格兰菲 2026Q1` 实跑一次做端到端验证（fixture 已就位）
3. 若改动落定且用户要求留痕：`/gh-commit` 或按既有 `-F` message file 方式 commit（双端同步后一起提交）
