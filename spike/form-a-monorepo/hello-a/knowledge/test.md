# Phase 0 Spike Knowledge Probe

This file lives at `${CLAUDE_PLUGIN_ROOT}/knowledge/test.md`.

If you can read this content, the `${CLAUDE_PLUGIN_ROOT}` variable resolved correctly
when used in a `Read` directive inside a plugin command body.

MAGIC_TOKEN: spike-form-a-knowledge-test-passed

The MAGIC_TOKEN above lets you confirm the file was actually read (not just the
existence check) by greping the conversation output for the token.
