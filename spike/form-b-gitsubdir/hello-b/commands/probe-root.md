---
name: probe-root
description: Phase 0 spike form B — verify git-subdir source schema works AND ${CLAUDE_PLUGIN_ROOT} resolves.
---

# Probe (form B)

Read `${CLAUDE_PLUGIN_ROOT}/knowledge/test.md` and quote the MAGIC_TOKEN line.

If the marketplace add step failed, you wouldn't be running this — just confirming form-B installs and runs:
> PASS: form B (git-subdir) works. Token: <quoted line>
