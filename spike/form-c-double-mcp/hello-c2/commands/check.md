---
name: check
description: Phase 0 spike form C — c2 echo (we don't need a second probe; c1's check is enough).
---

# C2: just exists

If you ran `/hello-c2:check` it confirms hello-c2 was installed. The real test is `/hello-c1:check`'s process count.
