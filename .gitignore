# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
package-lock.json

# Build artifacts
dist/
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Spike scratch
spike/.probe-tmp/
spike/**/.DS_Store

# Local env (never commit API keys)
.env
.env.local
.envrc

# Reference materials — real portfolio company artifacts (financials, internal reports, BPs).
# Used locally to anchor design/test work; MUST NOT enter the public repo.
docs/reference/

# Runtime workspace — generated reports, evidence, baseline state files.
# Per-developer; never commit.
workspace/
