<!-- Generated by analyst-pro-plugins/scripts/build-from-source.ts from ANALYST_PRO/electron/skills/memo/SKILL.md. Do not edit directly. -->
---
name: memo
description: 从积累的 Deal Memory 证据合成 IC 投资备忘录草稿。用户说"写 IC Memo"、"出一份备忘录"时触发。
argument-hint: '[公司名]'
model: claude-sonnet-4-6
allowed-tools: 'Read, Write, AskUserQuestion, Bash(jina:*)'
---
# IC Memo 草稿生成

为 $ARGUMENTS 生成一份 IC（投资委员会）备忘录草稿。


## Failure Mode Preflight (hard-fail by default)

Run these checks before Step 1; abort on any failure.

1. **`jina` CLI + `JINA_API_KEY` available**:
   - Run via Bash: `which jina && [ -n "${JINA_API_KEY}" ] && echo OK || echo FAIL`
   - On FAIL → output exactly:
     "本命令需要 jina-cli + JINA_API_KEY。请：
      pip install jina-cli
      export JINA_API_KEY=jina_xxxxxx
      然后重启 Claude Code 重试。"
     Then end the session — do NOT continue.

2. **Plugin-shipped knowledge files readable**: each knowledge file referenced below
   should be readable via `Read ${CLAUDE_PLUGIN_ROOT}/knowledge/<file>.md`. If a
   read fails, output "Plugin install may be corrupted (knowledge file missing).
   Please reinstall: /plugin uninstall <name> && /plugin install <name>." and end.

3. **CWD writable**: write a marker file `.analyst-write-test` then delete it.
   - On failure → switch to read-only mode (output report content to chat, do not
     write files). Tell the user explicitly that no files will be written.

4. **`./workspace/` directory check**: if `./workspace/` does not exist, ask the
   user via AskUserQuestion:
   - A) Create `./workspace/` in current directory (recommended)
   - B) Specify a different path
   - C) Skip workspace mode (write reports to `./reports/<slug>/` instead)


5. **Evidence file required**: this command synthesizes from accumulated evidence.
   - Verify `./workspace/state/<company-slug>/evidence.md` exists and is non-empty.
   - If missing or empty → HARD FAIL: "memo command needs prior evidence.
     Run `/analyst-deal:deal-analysis $ARGUMENTS` first to accumulate evidence."


## 执行步骤

1. Read `${CLAUDE_PLUGIN_ROOT}/knowledge/ic_memo_template.md` — 这是 IC Memo 的标准结构模板，必须严格遵循
2. Read `./workspace/state/$ARGUMENTS-slug/evidence.md` — 这是预编译的证据文件，包含所有已积累的实体和证据条目
3. 按照模板结构，逐章节填充证据内容：
   - **投资摘要**: 基于证据中的公司概况 + 得分 + 核心亮点/风险，撰写 3-5 句话的执行摘要
   - **投资逻辑**: 从 thesis 类型实体和高置信度证据提炼投资论点
   - **公司概况**: 使用 company 实体的 metadata（成立日期、注册地、赛道、定位）
   - **竞品分析**: 使用 technology 类型实体和行业对比证据
   - **风险矩阵**: 使用 risk 类型实体和 is_kill_flag=true 的证据条目
   - **估值分析**: 使用融资相关证据
   - **红队攻防**: 对每个核心投资论点，用低置信度证据和 kill flags 提出反面论证
   - **历史一致性检查**: 检查是否有与之前分析矛盾的证据
   - **投资建议**: 基于整体证据强度和得分给出明确建议

4. **内联引用**: 每个关键主张后标注来源和置信度，格式: `[来源名称, 置信度 0.X]`
5. **置信度缺口**: 明确列出缺失的重要维度，例如 "⚠ 未找到 TRL 评估数据" "⚠ 无 IP 景观分析"
6. 将草稿写入 `./workspace/state/$ARGUMENTS-slug/ic_memo_draft.md`

## HITL 审批

草稿生成后，通过 AskUserQuestion 展示备忘录摘要，提供以下选项：
- "批准并保存" — 备忘录存档到 ./workspace/state/deals/ic_memos/
- "需要修改" — 用户提供修改意见，重新生成
- "放弃草稿" — 不保存

## 注意事项

- 所有内容必须基于 evidence.md 中的实际证据，禁止虚构数据
- 置信度 ≤ 0.3 的证据必须标注 "⚠ 低可信度"
- Kill flag 证据必须在风险矩阵中突出展示
- 如果 evidence.md 文件不存在或为空，告知用户需要先运行 /deal 分析

## Output Location

Reports and evidence write to `./workspace/state/deals/<slug>/` in the user's current working
directory. If `./workspace/` was created in the preflight, this path is
relative to it. Use the company/project name as the slug (lowercase,
hyphen-separated, ASCII transliteration of CJK if applicable).

