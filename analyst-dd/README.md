# analyst-dd

This plugin's commands + knowledge files are **generated** by the marketplace transformer at `../scripts/build-from-source.ts`.

Run `npm run build:plugins:plugin analyst-dd` from the repo root to regenerate.

For end-user installation + setup, see the marketplace top-level [README.md](../README.md).
